{
  "name": "aperture-db",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "bun --watch apps/compiler/src/index.ts",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^3.0.0"
  }
}
